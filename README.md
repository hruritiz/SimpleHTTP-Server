# SimpleHTTP Server

This is a simple **HTTP server in C++** that serves files from a selected folder through a browser.

## Essentially:
> A local file server with a web interface and automatic directory listing.